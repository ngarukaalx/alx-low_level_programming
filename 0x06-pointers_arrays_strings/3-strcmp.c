#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strcmp - compares two strings
 * @s1:being checked
 * @s2:being checked
 *
 * Return: strcmp(s1, s2)
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
