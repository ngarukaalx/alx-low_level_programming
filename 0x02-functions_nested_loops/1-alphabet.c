#include "main.h"
#include <stdio.h>

/**
 * main -entry point
 *
 * Return:returns 0
 */

int main(void)
{
	char k;

	for (k = 'a'; k <= 'z'; k++)
	{
		putchar(k);
		}
	putchar('\n');
	return (0);
}
